#  Github

Maintenance notes for `pyxis3-ai/.github`.

## Maintenance

Supporting documentation lives in `docs/`, example inputs live in `examples/`, and lightweight validation notes live in `tests/smoke/`.
